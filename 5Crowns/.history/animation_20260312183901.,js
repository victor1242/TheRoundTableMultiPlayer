let timeScale = 1.0; // 0.5 = slow, 2.0 = fast
function update(deltaTime) {
    let scaledDelta = deltaTime * timeScale;
    player.x += player.speed * scaledDelta;
    // Update animations, physics, etc.
}

let isPaused = false;
let stepRequested = false;

function gameLoop(timestamp) {
    if (!isPaused || stepRequested) {
        update(1000/60); // Use fixed time step
        draw();
        stepRequested = false; // Reset step
    }
    if (!isPaused || stepRequested) {
        requestAnimationFrame(gameLoop);
    }
}

// Button handler
function onStepClick() {
    stepRequested = true;
    if (isPaused) requestAnimationFrame(gameLoop);
}

/*
JavaScript game speed control and stepping are implemented by manipulating the time delta (
) within the requestAnimationFrame loop, typically using a multiplier variable (e.g., 
 for normal speed, 
 for slow motion) to scale movement and physics. Stepping allows pausing the loop and advancing the game one frame at a time. 
YouTube
YouTube
 +3
Speed Control Implementation
Time Scale Modifier: Implement a global timeScale variable. Multiply all game object updates (movement, animations) by this variable within your loop.

GUI Slider: Use an HTML range input slider to dynamically change the timeScale variable.
Engine Specifics: If using a framework like Phaser or Godot, access the time_scale property directly. 
YouTube
YouTube
 +2
Stepping Option (Frame-by-Frame)
To implement stepping, the game loop must be paused and manually triggered.
Pause Loop: Set a boolean isPaused = true and stop calling requestAnimationFrame.
Step Function: Create a function that calls the update() and draw() functions exactly once, using a fixed delta time (e.g., 1000/60 ms for 60 FPS).
UI Button: Add a "Step" button that calls this function. 

Key Considerations
Physics Stability: When changing speed, high values can cause physics glitches. Implement a fixed time step for physics calculations to ensure stability.
Input Handling: Ensure keyboard/mouse inputs are still processed during slow-motion or paused states by checking input events outside the main if(isPaused) block.

*/

In JavaScript game development, speed control and stepping are essential for debugging and gameplay balancing. You can achieve this by manipulating the game loop's timing and logic. 
1. Game Speed Control (Time Scaling)
The standard way to control game speed is by using a timeScale variable. This multiplier is applied to your deltaTime (the time elapsed between frames), which affects movement, physics, and animations without changing the actual frame rate. 
YouTube
YouTube
 +3
Normal Speed: timeScale = 1.0
Slow Motion: timeScale = 0.5
Fast Forward: timeScale = 2.0
Paused: timeScale = 0.0 
Implementation Example:
javascript
*/

let timeScale = 1.0; // Adjustable via UI (e.g., a slider)
let lastTime = 0;

function gameLoop(currentTime) {
    const realDeltaTime = (currentTime - lastTime) / 1000; // Time in seconds
    lastTime = currentTime;

    // Apply the timeScale to the delta used for game logic
    const scaledDeltaTime = realDeltaTime * timeScale;

    update(scaledDeltaTime); // Logic uses the scaled time
    render();

    requestAnimationFrame(gameLoop);
}

/*
Use code with caution.

2. Stepping Option (Frame-by-Frame)
Stepping allows you to advance the game by exactly one frame (or a fixed time step) while the game is otherwise paused. This is invaluable for debugging frame-perfect physics or collisions. 
Game Development Stack Exchange
Game Development Stack Exchange
 +3
Pause Logic: When paused, your main game loop should stop calling the update() function but keep calling render() to maintain visual responsiveness.
Step Function: Manually trigger a single call to update() with a fixed delta (e.g., 

 of a second) when a "Step" button is clicked. 
Stack Overflow
Stack Overflow
 +4
Step Implementation:
javascript
*/

let isPaused = false;
const fixedDelta = 1 / 60;

function step() {
    if (isPaused) {
        update(fixedDelta); // Manually advance logic one "step"
        render();
    }
}

// In your main loop:
function gameLoop(currentTime) {
    // ... calculate realDeltaTime ...
    if (!isPaused) {
        update(realDeltaTime * timeScale);
    }
    render();
    requestAnimationFrame(gameLoop);
}

/*
Use code with caution.

3. Key Concepts for Smooth Control
Frame-Rate Independence: Always multiply movement values by deltaTime so the game runs at the same speed regardless of the user's monitor refresh rate (60Hz vs 144Hz).
Animation Scaling: Ensure your sprite animations also check timeScale so characters don't walk in place while time is slowed down.
UI Integration: Use a slider for the timeScale and a button for the "Step" function to make these tools accessible during development. 
*/